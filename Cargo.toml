[package]
name = "wat"
version = "0.1.5"
edition = "2024"

[dependencies]
notify-debouncer-mini = "0.6"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
globset = "0.4"
lexopt = "0.3"

[profile.release]
opt-level = 3
lto = true
strip = true
codegen-units = 1
panic = "abort"
