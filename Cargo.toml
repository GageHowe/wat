[package]
name = "wat"
version = "0.1.6"
edition = "2024"

[dependencies]
notify = "8"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
globset = "0.4"
lexopt = "0.3"

[profile.release]
opt-level = 3
lto = true
strip = true
codegen-units = 1
panic = "abort"
